# Seguridad de base de datos

La función privilegiada fija `search_path`, revoca ejecución web y recibe actor desde el backend. Las consultas usan tipos UUID/enum/numeric y no SQL dinámico. Constraints, FKs e índices aplican integridad además de la validación de aplicación.
