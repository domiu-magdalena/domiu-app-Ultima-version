# Eventos

Eventos persistidos:

- creación manual en `audit_log`;
- estado inicial y cambios en `order_tracking`;
- decremento/restauración en `manual_order_inventory_movements`;
- notificaciones y chats mediante triggers existentes;
- liquidaciones y pagos mediante triggers existentes.
